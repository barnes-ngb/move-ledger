# plans

Single-use build instructions. An agent reads one of these top to bottom, runs it, and the file is spent.

They are not specification, so they do not belong in `docs/`. They are not decisions, so they do not belong in `decisions/`. They live here because they record how the code got its shape, which is worth keeping after the run.

Two rules:

- Do not re-run a spent plan.
- Do not edit a plan to work around a problem found while running it. A failure during a run is a finding. Report it, decide, then fix the code through a normal branch and pull request.

## Numbering

Settled 2026-08-02. The vertical slice was one plan when APPLY-02 was written, then two, and is now three, so everything after it shifted by two. APPLY-02's comments still carry the old numbers and `src/ui/Home.tsx` already names APPLY-05 correctly. Spent plans are records rather than documents to maintain, so APPLY-02 is left alone and this table is the current numbering.

| Number | Scope | State |
| --- | --- | --- |
| APPLY-01 | Scaffold and domain core | Run 2026-08-01 |
| APPLY-02 | Firebase init, auth, repositories, rules | Run 2026-08-01 |
| APPLY-03 | Tailwind, PWA shell, auth gate, indicator, first deploy | Run 2026-08-02 |
| APPLY-04 | Move subscriptions, first-run setup, member onboarding | Run 2026-08-02 |
| APPLY-05 | The box flow. Add box, Find, box detail | Run 2026-08-02. **August 24 gate.** |
| APPLY-06 | Photo capture, resize, upload queue to Storage | Run 2026-08-15 |
| APPLY-07 | Cloud Function for the vision call, and the contents list in the app | Run 2026-08-15 |
| APPLY-08 | Word-by-word search, the box list, the full-screen photo | Run 2026-08-15 |
| APPLY-09 | Correcting a box. Void, delete, title, conditions, photo delete and retry | Run 2026-08-15 |
| APPLY-10 | Navigation, the doc 04 audit, and the export | Run 2026-08-15 |
| APPLY-11 | Corrections from use. The back gesture, photo capture, the contents list, room colors | Run 2026-08-16 |
| APPLY-12 | Suggestion visibility. The block on Add box, the marker on a list row | Run 2026-08-17 |

The box flow took APPLY-05 because build order and file numbers should agree, and the box flow is unambiguously next. Photos and the Cloud Function each shifted up one. APPLY-08 needed no renumbering: it was written after APPLY-07 ran and takes the next number in sequence, and APPLY-09 takes the one after it for the same reason.

## Runs

| Plan | Status | Notes |
| --- | --- | --- |
| `APPLY-01-domain-core.md` | Run 2026-08-01 | Verified on Node 22.22 before delivery against a Node 24 build machine. The flat `tsconfig.json` omitted `jsx`, `allowImportingTsExtensions`, and `vite/client`, so `tsc` failed on the template files it had pulled into `include`. Corrected during the run. |
| `APPLY-02-firebase-repositories.md` | Run 2026-08-01, fully applied | Rules tests passed on first execution anywhere: 9 of 9, no rule and no test weakened. Branched from `feat/domain-core` because APPLY-01 was unmerged at the time; both are merged now. Firestore rules deployed 2026-08-01, storage rules deployed 2026-08-02 once Storage was set up in the console. Defect found after the run: those 9 tests were 7 of doc 10's 9 required cases plus 2 extra Firestore cases. Closed 2026-08-02 by `tests/rules/storage.rules.test.ts`, which is a fix rather than a re-run. Nothing is owed from this plan. |
| `APPLY-03-pwa-shell.md` | Run 2026-08-02 | The first plan delivered without being compiled first. It compiled anyway: `tsc` clean, `vite build` clean, tests steady at 47, nothing in `src/domain` touched. Deployed to `https://move-ledger.web.app` and verified on an Android phone, including popup sign-in inside the installed standalone app. Two corrections during the run, neither from a compile error. `.gitignore` gained `tsconfig.tsbuildinfo` and `.firebase`. The sync indicator strings were wrong in the plan and in doc 04 both: doc 09 arbitrated to "Online" and "Offline, changes saved here", and doc 04 section 9 was amended to match. |
| `APPLY-04-first-run-setup.md` | Run 2026-08-02 | Move subscriptions, the first-run flow, and both halves of the ADR-0005 onboarding step. Delivered uncompiled and compiled clean on the first attempt: `tsc` clean, `vite build` clean, tests steady at 47, nothing in `src/domain` touched. Deployed with the cache headers verified live against the deployed site rather than trusted from the config. Deferred Add box, Find, and box detail to APPLY-05 by design. Left the Firebase bundle unsplit, which APPLY-03 had asked for before real screens landed, so that debt grew by 28 kB gzipped. |
| `APPLY-05-box-flow.md` | Run 2026-08-02 | Add box, Find, box detail, the bundle split, and the first render environment. Delivered uncompiled. `tsc` clean on the first attempt, so nothing in the plan's TypeScript was wrong. One correction, and it was the test harness rather than the code: Testing Library registers its `cleanup` only when a global `afterEach` exists, and `vitest.config.ts` as the plan wrote it does not enable globals, so the second `render` in `FindBox.test.tsx` stacked on the first and `getByText("4")` found two keypads. Added `globals: true`, which also makes the runtime match the `vitest/globals` entry `tsconfig.json` already carried. Tests went from 47 to 56. Deployed to `https://move-ledger.web.app`. Not yet run on a phone, so the two numbers the gate measures are still unmeasured. |

| `APPLY-06-photo-capture.md` | Run 2026-08-15 | Photo capture, resize, the Dexie queue, and the uploader, on top of an offline write fix that took the first half of the run. The defect was confirmed before it was fixed, with the real SDK under `disableNetwork`: a Firestore write promise settles on server acknowledgment, so every screen in the app hung offline while the local cache already held its data. See the first entry under Resolved drift in `STATUS.md`. Delivered uncompiled and compiled clean on the first attempt. Four deviations from the plan, all recorded in the pull request: `createValidated` and `updateValidated` return `{ value, written }` rather than a promise, which is the shape the plan asked for without naming; `addPhotoRecord` takes the id and no longer generates one; the uploader imports `getPhotoRecord` statically rather than through a dynamic import; and Dexie was given its own bundle chunk. Tests went from 76 to 95. The success criteria were rewritten in the same run. |

| `APPLY-07-contents-summary.md` | Run 2026-08-15 | The callable function holding the Anthropic key, and the contents list in the app. Doc 07 was amended first, as the plan required: one photo per box became every contents photo, and the cap went from 400 to 800. Delivered uncompiled and did not compile: the text-block filter used a type predicate that TypeScript rejects, because the SDK's `TextBlock` carries a required `citations` field the asserted type omitted. Three other corrections during the run, all recorded in the pull request. The model was changed twice, from the plan's dated Haiku string to the undated alias and then to `claude-sonnet-5` on request, which forced `thinking: disabled` because Sonnet 5 thinks by default and `max_tokens` caps thinking and reply together. Function deployed to `us-central1`. Tests went from 95 to 109. The first real call has not happened: it needs a phone. |

| `APPLY-08-find-and-review.md` | Run 2026-08-15 | Token search, the box list, and the full-screen photo. Written against a real summary the deployed function produced for box 015, which "travel leisure" could not find. Delivered uncompiled and compiled clean on the first attempt: `tsc` clean, `vite build` clean, and all five existing search tests passed the rewrite unchanged. Two corrections during the run, neither of them a compile error in the plan's own TypeScript. The plan's `tokenMatches` comment claimed "toys" finds "toy", which its own `MIN_PREFIX` of 4 forbids, so the comment was corrected and the code was taken verbatim. And a test helper widened `onOpen` to a union that has no `.mock`, which `tsc` caught. Two further changes came out of review on the pull request rather than the run. The new button read "All boxes", a noun phrase where `AGENTS.md` requires a verb, and is now "See all boxes". And `tokenize` deviated from the plan's verbatim code on a decision taken in the pull request: `[^a-z0-9]+` deletes every character of a non-Latin script, so text in one tokenized to nothing and became unfindable, which the substring search it replaced had handled. Accents are folded before the split, which also keeps "jalapeno" finding "jalapeño", a match the plan's tokenizer got by accident and the reviewer's suggested fix would have lost. Tests went from 109 to 145. Not deployed: CI owns Hosting. |

| `APPLY-09-correcting-a-box.md` | Run 2026-08-15 | Void and delete, title, conditions, photo delete and retry. The first plan to arrive in two parts: steps 0 to 2 were built and pushed, then two corrections came back with step 3 and are recorded as amendments at the foot of the plan rather than folded into the steps they change. Both corrections were the same mistake in two places. The first draft awaited the Cloud Storage delete so the ordering in the plan would be literally sequential, which offline leaves a deleted photo on screen for about two minutes and contradicts the rule the whole app is built on. It now fires and does not wait, and the orphaned objects that produces are accepted and recorded in `STATUS.md`. The second added the box's photos to `deleteContainer`, which had deleted only the container document: capture writes a photo document before the box is saved, so a deletable draft is exactly the kind of box that can be holding photos nothing else would clean up. Delivered uncompiled both times. The only compile error in either part was a test file the signature changes had outrun. Three judgment calls in step 3 are in the pull request description. Tests went from 145 to 202. Not deployed: CI owns Hosting. |

| `APPLY-10-navigation-and-polish.md` | Run 2026-08-15 | One navigation pattern, an audit of every screen against doc 04, and the export. Navigation had grown a screen at a time and had no way home from four screens, no way out of Add box at all, and one word meaning two things. The pattern is in doc 04 section 0 and is settled: the title is the way home, back sits top left above the scroll area and returns to where the screen was opened from, Done is only for completing. `src/ui/nav.tsx` is the one piece that needed inventing, because the header is rendered above every screen while the state that decides where home is lives below it. Delivered uncompiled. One compile error and it was in the test files rather than the plan's own work: renaming `onClose` to `onBack` on three components outran the three test files that construct them. The audit found more than the brief named. The keyboard was covering `Save and next` on every screen with a field, which the pinned footer could not fix on its own and one line of viewport meta did, and the move overview was showing zeroed counters to a person who had not started, which doc 04 section 1 forbids in as many words. Both are recorded in doc 04 with the rest of the audit, fixed and compliant alike. The export was reachable from nowhere since APPLY-01 and is now two buttons on the rooms screen. Tests went from 204 to 226, including the first tests of `AddBox`. Not deployed: CI owns Hosting. |

| `APPLY-11-corrections-from-use.md` | Run 2026-08-16 | Four findings from the first real use of the installed app, two of them regressions. The back gesture closed the app from every screen, because the view was a `useState` switch with no History API under it and a standalone PWA has no browser chrome to catch the pop. Every view change now pushes an entry and the pop is what changes the view, in `src/ui/history.ts`, with the on-screen Back and the header title travelling the same path. Photo capture lost the camera when APPLY-09 removed `capture="environment"`: the attribute is a hint rather than a contract, and without it Android offered files only, so the strip now carries two controls and one input each. The contents list became editable on box detail through `setContentsSummary`, which goes through `saveContainer` so `searchText` is rebuilt. And a room's color can be changed from the named palette, which product rule 3 has always allowed and nothing had implemented. Delivered uncompiled and compiled clean on the first attempt: `tsc` clean, `vite build` clean, no correction needed to the plan's own work. Tests went from 228 to 268, including the first tests `PhotoStrip`, `Rooms`, and `Home` have ever had. Three judgment calls are in the pull request description. Not deployed: CI owns Hosting. |

| `APPLY-12-suggestion-visibility.md` | Run 2026-08-17 | The generated contents list, made visible in the two places a person stands. The suggestion block had been mounted on box detail only since APPLY-07, and a summary is written in the background after the photo uploads, so it usually lands while the same box is still being packed on a screen that did not render it. It is now below the photos on Add box, the same component rather than a fork, and both save buttons are unchanged. The box list marks a row carrying a suggestion nobody has answered, with nothing typed, which the old marker could not do because it fired on a property of the query. Two things came out of the run that the brief did not name. Add box had to start reading the live container rather than the copy reserved on mount, which is the only reason anything written to a box while that screen is open can reach it. And basing the save on that live copy fixed a `searchText` defect nobody had reported: `saveContainer` rebuilds the index from what it is handed, so saving the stale copy took a summary's own words back out of it. Delivered uncompiled and compiled clean on the first attempt: `tsc` clean, `vite build` clean, no correction needed to the plan's own TypeScript. One change came out of review on the pull request rather than the run, and it is a defect the plan itself introduced: saving spreads the whole container, so basing that on the live copy meant answering a suggestion and saving in the same moment wrote the suggestion back over the answer. `AiSummary` now hands back the container accept and dismiss wrote, through an optional callback box detail does not pass, and Add box holds it above the subscription until the write lands. How long it is held took two attempts: ordering the copies by `updatedAt` compares the phone's clock against the function's, so a phone running slow would discard the answer and write the suggestion back, and the second attempt holds the answer while the subscription is still showing the exact text that was answered. Both are recorded as one amendment at the foot of the plan. Tests went from 268 to 285. One judgment call is recorded in the pull request and one finding is left open for a phone: the block sits between the photos and the room picker, so a summary arriving while somebody is typing the note moves the field under their thumb. Not deployed: CI owns Hosting. |

Update the status column in the same pull request as the run.
